#include<stdio.h>
int main(){

        int n, n2;
        // scanf() returns the number of successful integer values scanned whatever may be the input, in this case it is 2
        printf("%d\n",scanf("%d %d",&n,&n2));
        return 0;
}
